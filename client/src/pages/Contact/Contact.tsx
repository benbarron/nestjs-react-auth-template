import React, { Fragment } from "react";
import { withRouter } from "react-router";

interface Props {}

export const Contact = withRouter((props: Props) => {
  return (
    <Fragment>
      <h1>Contact</h1>
    </Fragment>
  );
});
